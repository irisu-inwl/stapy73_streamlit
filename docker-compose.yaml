version: '3'
services:
  streamlit-app:
    image: streamlit-demo 
    build: .
    #command: streamlit run src/test_st.py
    ports:
      - 8501:8501
    volumes:
      - ./src/:/opt/streamlit/src/